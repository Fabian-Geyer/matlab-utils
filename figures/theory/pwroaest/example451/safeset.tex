\begin{tikzpicture}

\begin{axis}[
width=0.8\FigWidth,
height=\FigHeight,
at={(0\FigWidth,0\FigHeight)},
scale only axis,
%extra x ticks={0},
%extra x tick style={major grid style={dashed, color=black}},
%extra y ticks={0},
%extra y tick style={major grid style={dashed, color=black}},
xtick={-1.5,-1,...,1.5},
ytick={-1.5,-1,...,1.5},
xlabel={$x_1$},
ylabel={$x_2$},
xmajorgrids,
ymajorgrids,
ymin=-1.25,
ymax=1.25,
xmin=-1.25,
xmax=1.25,
restrict expr to domain={x^2+y^2}{0:10},
view={0}{90},
unbounded coords=jump,
colormap name=reverse jet,
]
\foreach \i in {1,2,...,4}
	\addplot3[contour gnuplot, safeSet, dashed,
	contour external/output point meta={\i},
	contour/labels=true, contour/label distance=500pt,
	mesh/rows=51, mesh/cols=51,
	]
	table[x=x1,y=x2,z=Lya\i]{\data/theory/pwroaest/example451.dat};

\addplot3[contour gnuplot, safeSet,
contour external/output point meta=5,
mesh/rows=51, mesh/cols=51,
]
table[x=x1,y=x2,z=Lya]{\data/theory/pwroaest/example451.dat};

\end{axis}
\end{tikzpicture}