\begin{tikzpicture}

\begin{groupplot}[%
group style={
	group size=2 by 2,
	horizontal sep=1pt,
	vertical sep=1pt,
	x descriptions at=edge bottom,
	y descriptions at=edge left,
},
width=0.4\FigWidth,
height=0.5\FigHeight,
at={(0\FigWidth,0\FigHeight)},
scale only axis,
xlabel={$x_1$},
ylabel={$x_2$},
grid=both,
view={0}{90},
xmin = -3, xmax = 3,
ymin = -10, ymax = 10,
xtick = {-2.5,0,2.5},
ytick = {-8,0,8},
minor tick num = 1,
empty legend,
]

\nextgroupplot
\foreach \i in {1,2,3,4,5,6,7,8,9}
{
\addplot3[contour gnuplot, safeSet color=mycolor1, line width=0.5, %dashdotdotted,
mesh/rows=100, mesh/cols=100,
]
table[x=x, y=y, z=vbar]{\data/theory/scenarioopt/mpc/scenarioopt1-\i.dat};

\legend{$q = 1$}
}

\nextgroupplot
\foreach \i in {1,2,3,4,5,6,7,8,9}
{
\addplot3[contour gnuplot, safeSet color=mycolor2, line width=0.5, %dashdotted,
mesh/rows=100, mesh/cols=100,
]
table[x=x, y=y, z=vbar]{\data/theory/scenarioopt/mpc/scenarioopt2-\i.dat};

\legend{$q = 2$}
}

\nextgroupplot
\foreach \i in {1,2,3,4,5,6,7,8,9}
{
\addplot3[contour gnuplot, safeSet color=mycolor3, line width=0.5, %dashed,
mesh/rows=100, mesh/cols=100,
]
table[x=x, y=y, z=vbar]{\data/theory/scenarioopt/mpc/scenarioopt3-\i.dat};

\legend{$q = 3$}
}

\nextgroupplot
\foreach \i in {1,2,3,4,5,6,7,8,9}
{
\addplot3[contour gnuplot, safeSet color=mycolor5, line width=0.5, %solid,
mesh/rows=100, mesh/cols=100,
]
table[x=x, y=y, z=vbar]{\data/theory/scenarioopt/mpc/scenarioopt4-\i.dat};

\legend{$q = 4$}
}

\end{groupplot}
\end{tikzpicture}
